# Removed. The PrioritizedSCP row-normalization issue is now diagnosed and
# fixed via the `prio_scp_normalize_obstacles` field in SolverConfig (set to
# False in configs/sweep_n_prioritized.yaml). This diagnostic CLI is no longer
# needed.
#
# This file can be `git rm`'d on the next commit.
